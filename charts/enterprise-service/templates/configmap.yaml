{{- if .Values.config.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "enterprise-service.fullname" . }}-config
  namespace: {{ .Values.namespace | default .Release.Namespace }}
data:
  {{- toYaml .Values.config.data | nindent 2 }}
{{- end }}
